# 2026-08-05

## 项目学习文档编写
- 通读「空库」项目（个人认知知识库：FastAPI + React19/Vite + Electron + SQLite，RAG 只按库内答、可拒答），完成全项目代码探索（借助 Explore 子代理）。
- 产出 `docs/study/` 学习文档共 16 篇：
  - `README.md`：项目总览（是什么、12 项核心功能、喂养/对话/桌面端三大业务流程、目录速查、文档导航）
  - `技术框架.md`：24+ 项框架/技术栈（官网、项目内用途、使用说明），按前端/后端/桌面端/AI/解析库/工程化分组
  - `功能点/01~14`：每功能独立成篇（实现逻辑 + 关键代码路径 + 配置说明）
- 项目要点备忘：API 端口 18765、Vite 41779、抖音桥 18766；DB 优先级 runtime-db.json > DATABASE_URL > data/kongku.db；向量存 chunks.embedding 为 JSON 字符串（无 pgvector 依赖）；技能包禁脚本且 knowledge_policy=library_only；产品文档中的「导出/备份」在 API 中尚未实现（data/exports 为空，属规划功能）。

## 文档 0→1 重构（同日第二轮）
- 按用户要求将 docs/study/ 全部 16 篇重构为「从零到一」教程体：每篇结构 = 解决什么问题 → 先修概念 → 从零推导（朴素实现→坑→项目解法）→ 真实代码片段逐段解读（为什么这样写/作用/设计考量）→ 流程图 → 文件配置对照表。
- 先用 5 个 Explore 子代理并行精读源码取证（api core/settings、sources、knowledge/chat、vault/open_books/skills/library、web/desktop），再逐篇重写。README 增加 0→1 推荐阅读路径。
- 用户新需求方向（候选）：PDF 预览器内做笔记（应对图表 OCR 乱码）——已给可行性分析：pdf.js 渲染 + 划句批注 + 区域截图 OCR 人工确认，与现有批注/跟读模式同构，可渐进实施。
- 已落地 P0+P1：`GET /api/sources/{id}/original`；批注扩 `page`/`rect_json`；前端 `PdfPreviewModal`（pdf.js 框选页注）；书架/知识/喂养 PDF 默认开原件，可切抽取正文。OCR 入库仍保留。
